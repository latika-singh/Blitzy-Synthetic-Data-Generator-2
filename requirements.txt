# =============================================================================
# Synthetic ERP Data Generation Platform — Project 2
# Agent & Orchestration Engine — Production Dependencies
# =============================================================================
# This file lists all 18 production packages required by the engine.
# Dev/test dependencies are in requirements-dev.txt.
# Version specifiers sourced from the project specification (README.md).
# =============================================================================

# -----------------------------------------------------------------------------
# LLM Provider SDKs
# -----------------------------------------------------------------------------
anthropic>=0.79.0          # Anthropic Claude API async client (AsyncAnthropic) for LLM completions
openai>=2.20.0             # OpenAI / Azure OpenAI async client (AsyncOpenAI) for LLM completions

# -----------------------------------------------------------------------------
# Async and Networking
# -----------------------------------------------------------------------------
aiohttp>=3.9.0             # Async HTTP client for Project 1 REST API integration

# -----------------------------------------------------------------------------
# Data Storage
# -----------------------------------------------------------------------------
redis>=7.0.0               # Redis client for caching, LLM queue (Streams), event store, agent state

# -----------------------------------------------------------------------------
# Scientific Computing
# -----------------------------------------------------------------------------
numpy>=1.26.0              # Array operations, random state seeding for reproducible simulations
scipy>=1.12.0              # Statistical distributions: lognorm, norm, poisson, pareto via scipy.stats

# -----------------------------------------------------------------------------
# Data Generation and Utilities
# -----------------------------------------------------------------------------
faker>=22.0.0              # Synthetic data generation with custom ERP providers
pydantic>=2.5.0            # Data validation at all subsystem boundaries (V2 with BaseModel)
structlog>=24.1.0          # Structured JSON logging to stdout
python-dateutil>=2.8.0     # Date arithmetic, relative deltas for business calendar operations
holidays>=0.40             # US Federal holiday calendar (2024-2026)
tenacity>=8.2.0            # Retry with exponential backoff for LLM API calls

# -----------------------------------------------------------------------------
# ML / Embeddings
# -----------------------------------------------------------------------------
sentence-transformers>=2.3.0  # Local all-MiniLM-L6-v2 embeddings for agent memory semantic retrieval

# -----------------------------------------------------------------------------
# Project 3: Transaction Workflows & Database
# -----------------------------------------------------------------------------
sqlalchemy==2.0.25         # ORM and async database access for transaction persistence via Project 1's get_session()
psycopg2-binary==2.9.9     # PostgreSQL adapter for SQLAlchemy async sessions (Project 1 database)
aiofiles==23.2.1           # Async file I/O for ground truth JSON/CSV output files
pydantic-settings==2.2.0   # Environment variable loading for P3 configuration (TRANSACTION_BATCH_SIZE, etc.)
pandas==2.2.0              # DataFrame operations for ground truth summary CSV generation and batch analytics
